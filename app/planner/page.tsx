export default function PlannerPage() {
  return (
    <div className="content-inner">
      <h1 style={{ fontSize: 'var(--text-xl)', fontWeight: 700, color: 'var(--text-primary)', marginBottom: 8 }}>
        Planner
      </h1>
      <p style={{ color: 'var(--text-muted)', fontSize: 'var(--text-sm)' }}>
        Time blocking and scheduling — coming soon.
      </p>
    </div>
  );
}
